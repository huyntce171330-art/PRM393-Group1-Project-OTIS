// This file defines the states for the ProfileBloc.
//
// Steps to implement:
// 1. Define an abstract class `ProfileState`.
// 2. Create `ProfileInitial` state.
// 3. Create `ProfileLoading` state.
// 4. Create `ProfileLoaded` state.
//    - Should contain the `User` object: `final User user;`
// 5. Create `ProfileError` state.
//    - Should contain the error message: `final String message;`
