// This file defines the events for the ProfileBloc.
//
// Steps to implement:
// 1. Define an abstract class `ProfileEvent`.
// 2. Create a class `GetProfileEvent` extending `ProfileEvent`.
// 3. Create a class `UpdateProfileEvent` extending `ProfileEvent`.
//    - This event should hold the data to be updated (e.g., `final User user;`).
