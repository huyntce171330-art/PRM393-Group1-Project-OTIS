// Widget to display a single cart item.
//
// Steps:
// 1. Accept `CartItem` as parameter.
// 2. Display Product Image, Name, Price.
// 3. Add `Row` with "-", Quantity, "+" buttons.
//    - "+" triggers `UpdateCartItemEvent` (qty + 1).
//    - "-" triggers `UpdateCartItemEvent` (qty - 1).
// 4. Add "Delete" icon to trigger remove event.
