// Use case to add a product to the cart.
//
// Steps:
// 1. Create `AddProductToCartUsecase`.
// 2. Inject `CartRepository`.
// 3. Define `call` method taking `productId` and `quantity`.
// 4. Invoke `repository.addToCart(...)`.
