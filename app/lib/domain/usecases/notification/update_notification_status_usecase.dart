// Use case to update notification status (e.g., mark as read).
//
// Steps:
// 1. Create `UpdateNotificationStatusUsecase`.
// 2. Inject `NotificationRepository`.
// 3. `call` method accepts `String id` and `bool isRead`.
// 4. Invoke `repository.updateNotificationStatus(id, isRead)`.
